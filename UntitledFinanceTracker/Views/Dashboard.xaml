<UserControl x:Class="UntitledFinanceTracker.Views.Dashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UntitledFinanceTracker.Views"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Initialized="UserControl_Initialized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2" Margin="5 10 0 0">
            <Label Margin="5 0 5 0" Content="From" FontSize="11" />
            <DatePicker x:Name="dtFrom" Width="105" />
            <Label Margin="5 0 5 0" Content="To" FontSize="11"  />
            <DatePicker x:Name="dtTo" Width="105" />
        </StackPanel>
        
        <DockPanel Grid.Row="1" Grid.Column="0" LastChildFill="True" Margin="10" Background="#212121">
            <Label DockPanel.Dock="Top" HorizontalAlignment="Center" VerticalAlignment="Top">Weekly Balance</Label>
            <lvc:CartesianChart x:Name="chrtBalance" Series="{Binding series}" DisableAnimations="True" DataTooltip="{x:Null}"
                                Margin="0 -10 20 20" Padding="10">
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis x:Name="chrtBalanceAxisY" LabelFormatter="{Binding Formatter}">
                        <lvc:Axis.Separator>
                            <lvc:Separator Stroke="#707070"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisY>
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis x:Name="chrtBalanceAxisX" MinValue="1">
                        <lvc:Axis.Separator>
                            <lvc:Separator Stroke="#707070"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
            </lvc:CartesianChart>
        </DockPanel>
    </Grid>
</UserControl>
